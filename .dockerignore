# Exclude everything by default
*

# The image reads its binary from $TARGETPLATFORM/, so the build context is a
# per-platform tree (linux/amd64/terratidy, linux/arm64/terratidy). GoReleaser
# assembles that layout itself; the local and CI builds mirror it.
!linux
