resource "aws_instance" "web" {
  ami = "ami-123"

  lifecycle {
    create_before_destroy = true
    ignore_changes        = [tags]

    precondition {
      condition     = var.ami != ""
      error_message = "ami required"
    }
  }
}
